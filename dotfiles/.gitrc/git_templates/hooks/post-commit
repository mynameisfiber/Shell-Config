### lolcommits hook (begin) ###
GIT_DIR=${GIT_DIR-`git rev-parse --git-dir`} && LANG="en_US.UTF-8" && PATH="$PATH:/home/micha/.local/share/mise/installs/ruby/latest/bin:/usr/sbin" && if [ ! -d "$GIT_DIR/rebase-merge" ] && [ "$LOLCOMMITS_CAPTURE_DISABLED" != "true" ]; then lolcommits --capture ; fi
###  lolcommits hook (end)  ###
