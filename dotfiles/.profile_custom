[ -n "${LOADED_PROFILE_CUSTOM_INNER}" ] && return 0
LOADED_PROFILE_CUSTOM_INNER=1

export PATH="/usr/sbin:$PATH"

for script in "$HOME"/.profile_scripts/*; do
    [ -r "$script" ] && [ -f "$script" ] && . "$script"
done
